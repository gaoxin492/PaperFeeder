# Daily Paper Assistant Configuration
# Copy this file to config.yaml and customize

# =============================================================================
# arXiv categories to monitor
# =============================================================================
arxiv_categories:
  - cs.LG   # Machine Learning
  - cs.CL   # Computation and Language
  - cs.CV   # Computer Vision
  - cs.AI   # Artificial Intelligence
  - stat.ML # Statistics - Machine Learning

# =============================================================================
# Keywords for filtering (matches title + abstract)
# Papers matching ANY keyword will be included
# =============================================================================
keywords:
  # Generative models
  - diffusion model
  - diffusion language
  - flow matching
  - generative model
  - autoregressive
  
  # LLM reasoning
  - chain of thought
  - reasoning
  - large language model
  - in-context learning
  
  # Representation learning
  - representation learning
  - contrastive learning
  - self-supervised
  - foundation model
  
  # AI Safety
  - ai safety
  - alignment
  - rlhf
  - red teaming
  - jailbreak
  - safety benchmark
  
  # Specific interests
  - tokenizer
  - tokenization
  - continuous token
  - latent space
  - latent reasoning

# Keywords to exclude
exclude_keywords: [3D, medical, 4D, video]

# =============================================================================
# Your research interests (for LLM filtering and summarization)
# =============================================================================
research_interests: |
  I'm a Master's student researching:
  1. Generative models, especially diffusion models for language
  2. LLM reasoning, including chain-of-thought and latent reasoning
  3. Representation learning and continuous tokenization
  4. AI safety, including benchmarks and alignment
  
  I'm particularly interested in papers that:
  - Bridge generation and representation learning
  - Propose new reasoning paradigms for LLMs
  - Introduce novel safety evaluation methods
  - Have strong mathematical foundations

# =============================================================================
# Filtering settings
# =============================================================================
llm_filter_enabled: true     # Enable LLM-based filtering (推荐开启)
llm_filter_threshold: 3      # 关键词筛选后，如果论文数 > N，则使用LLM筛选
max_papers: 3               # Maximum papers in final report

# =============================================================================
# PDF Multimodal Input (直接传PDF给模型，更高效)
# =============================================================================
extract_fulltext: true      # 是否使用PDF多模态输入（直接传PDF文件，不提取文本）
                             # 如果为true且模型支持（Claude/Gemini），会直接传PDF，token更少
                             # 如果为false，只使用摘要（abstract）
fulltext_top_n: 3            # 已废弃，保留用于兼容性
pdf_max_pages: 15            # PDF最大页数（0=全部，默认10页）
                              # 只提取前N页传给LLM，避免附录太长浪费token

# =============================================================================
# Manual paper source
# =============================================================================
manual_source_enabled: true
manual_source_path: "manual_papers.json"
